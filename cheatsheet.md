# Racket - основни блокчета

Вградени форми и функции.

## Специални форми

```racket
(define size 2)
size                         ; → 2
(define (square x) (* x x))
(square 3)                   ; → 9

(if (< 3 0) (- 3) 3)         ; → 3
(if (< -3 0) (- -3) -3)      ; → 3

(cond [(> 5 10) 0]
      [(odd? 5) 6]
      [else 5])              ; → 6

(and #t #f)                  ; → #f
(or #f #t)                   ; → #t
(not #t)                     ; → #f

((λ (x) (* x x)) 4)          ; → 16
((lambda (x) (* x x)) 4)     ; → 16. същото
```

`define`, `if`, `cond`, `and`, `or` и `lambda` не са обикновени функции.
Не оценяват всички аргументи по едно и също правило.

## Числа

```racket
(+ 1 2 3)          ; → 6
(- 5 3)            ; → 2
(* 2 4)            ; → 8
(/ 10 2)           ; → 5
(/ 10 4)           ; → 5/2
(remainder 10 4)   ; → 2
(quotient 10 3)    ; → 3
(add1 4)           ; → 5
(sqr 5)            ; → 25
```

## Сравнения и предикати върху числа

```racket
(= 3 3)            ; → #t. няма ==
(= 3 4)            ; → #f
(< 1 2)            ; → #t. още: <=  >  >=
(<= 2 2)           ; → #t
(odd? 11)          ; → #t
(even? 10)         ; → #t
(even? 11)         ; → #f
```

## Равенство

```racket
(= 2 2.0)                      ; → #t. числа
(eq? 'a 'a)                    ; → #t. същият обект
(eq? (list 1) (list 1))        ; → #f
(eqv? 2 2)                     ; → #t
(eqv? 2 2.0)                   ; → #f
(equal? '(1 2) (list 1 2))     ; → #t. структурно
```

`#t` и `#f` са истина и лъжа. В `if` и `cond` само `#f` е лъжа.

## Списъци и двойки

```racket
'()                            ; → '()
(cons 1 2)                     ; → '(1 . 2)
(cons 1 '(2 3))                ; → '(1 2 3)
(list 1 2 3)                   ; → '(1 2 3)

(car '(1 2 3))                 ; → 1
(cdr '(1 2 3))                 ; → '(2 3)
(caar '((1 . 2)))              ; → 1
(cdar '((1 . 2)))              ; → 2
(first '(1 2 3))               ; → 1
(second '(1 2 3))              ; → 2
(third '(1 2 3))               ; → 3
(rest '(1 2 3))                ; → '(2 3)

(pair? '(1 . 2))               ; → #t
(pair? '())                    ; → #f
(list? '(1 2))                 ; → #t
(list? '(1 . 2))               ; → #f
(empty? '())                   ; → #t
(empty? '(1 2))                ; → #f

(append '(1 2) '(3 4))         ; → '(1 2 3 4)
(reverse '(1 2 3))             ; → '(3 2 1)
(length '(1 2 3))              ; → 3
(take '(0 1 2 3 4) 3)          ; → '(0 1 2)
(drop '(0 1 2 3 4) 3)          ; → '(3 4)
(member 2 '(1 2 3))            ; → '(2 3)
(member 5 '(1 2 3))            ; → #f
(range 1 5)                    ; → '(1 2 3 4)
(sort '(6 3 2) <)              ; → '(2 3 6)
```

## Функции от по-висок ред

```racket
(map (λ (x) (* x 2)) '(1 2 3 4))   ; → '(2 4 6 8)
(filter odd? '(1 2 3 4 5))         ; → '(1 3 5)
(foldl cons '() '(1 2 3))          ; → '(3 2 1)
(foldr cons '() '(1 2 3))          ; → '(1 2 3)
(apply + 10 '(1 2 3 4))            ; → 20
(count even? '(0 1 2 3 4))         ; → 3
((curry + 1) 4)                    ; → 5
```

## Низове

```racket
(string-append "1" "1")        ; → "11"
(number->string 2)             ; → "2"
```
